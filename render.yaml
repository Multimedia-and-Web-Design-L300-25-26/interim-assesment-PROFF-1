services:
  - type: web
    name: coinbase-clone-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # Add the rest of your .env vars in the Render dashboard
