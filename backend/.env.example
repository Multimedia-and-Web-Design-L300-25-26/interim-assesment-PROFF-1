/*
 * Example environment variables for the Coinbase clone backend.
 * Copy these values into a local .env file before running the server.
 */
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/coinbase_clone
JWT_SECRET=replace_with_a_strong_secret
CLIENT_URL=http://localhost:5173
NODE_ENV=development